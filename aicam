#!/usr/bin/env bash
# Launcher: prefer the project venv, which is where mediapipe lives.
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python="$here/.venv/bin/python"
[ -x "$python" ] || python="$(command -v python3)"
exec "$python" "$here/aicam.py" "$@"
